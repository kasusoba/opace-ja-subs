# media (copy these to the PC manually — too big for git)
*.mp4
*.mkv
*.wav
*Zone.Identifier

# regenerable caches
work/

# regenerable per-episode trace artifacts (the .ja.ass deliverable, .vtt sources
# and override files are tracked; these two are just rebuilt by a run)
*.ja.ass.debug.tsv
*.ja.ass.review.json

# local tooling
.venv/
.claude/
__pycache__/
.DS_Store
